import 'package:flutter/material.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Altijd Halal Supermarkt',
      home: Scaffold(
        appBar: AppBar(
          title: Text('Altijd Halal Supermarkt'),
        ),
        body: Center(
          child: Text(
            'Welkom bij Altijd Halal Supermarkt!',
            style: TextStyle(fontSize: 24),
          ),
        ),
      ),
    );
  }
}
